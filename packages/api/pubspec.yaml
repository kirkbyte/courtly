name: courtly_api
description: API contracts and fake implementations for Courtly.
publish_to: none
environment:
  sdk: ^3.6.0
resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
